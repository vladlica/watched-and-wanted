import styled from "styled-components";

const TableSvgContainer = styled.div`
  & svg {
    width: 2rem;
    height: 2rem;
    color: var(--color-orange-600);
  }
`;

export default TableSvgContainer;
